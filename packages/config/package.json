{
  "name": "@multiportal/config",
  "version": "0.4.0",
  "private": true,
  "description": "Shared environment configuration validation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "pnpm run lint",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0"
  }
}
