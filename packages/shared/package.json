{
  "name": "@multiportal/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types, DTOs, enums, and validation schemas",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo ok",
    "test": "echo ok",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@types/node": "^26.1.0",
    "zod": "^4.0.0"
  }
}
