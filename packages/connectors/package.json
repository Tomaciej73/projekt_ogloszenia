{
  "name": "@multiportal/connectors",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-agnostic connector interface and marketplace connector implementations",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "pnpm run lint",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@multiportal/shared": "workspace:*"
  }
}
